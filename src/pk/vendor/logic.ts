// Re-export the shared domain model from dedicated files so existing imports
// from `logic.ts` keep working while we progressively untangle responsibilities.
export { Route, Ester, ExtraKey, type DoseEvent, type SimulationResult, type LabResult, type ConcUnit, type CompoundSeries } from './types';
export {
    getToE2Factor,
    SL_TIER_ORDER,
    SublingualTierParams,
    getBioavailabilityMultiplier,
    runSimulation,
    weightAtTimeH,
    interpolateConcentration,
    interpolateConcentration_E2,
    interpolateConcentration_CPA,
    interpolateCompoundConcentration,
    isAntiandrogen,
    pickPrimaryAntiandrogen,
    formatAntiandrogenConc,
    bicalutamideConcNgML,
    computeBicalutamideAmount,
    findPatchRemovalForApply,
    patchInstanceIdOf,
    ANTIANDROGENS,
    ANTIANDROGEN_ESTERS,
    BICA_PK,
    type AntiandrogenSpec,
    GelSite,
    GEL_SITE_ORDER,
    GEL_SITE_FACTORS,
    GEL_PRODUCTS,
    GEL_DEFAULT_PRODUCT_ID,
    getGelProduct,
    getGelProductById,
    gelProductExists,
    setCustomGelProducts,
    sanitizeGelProduct,
    sanitizeGelProducts,
    GEL_CUSTOM_ID_BASE,
    resolveGelKinetics,
    gel3CompCentralAmount,
    gelEventCentralAmount,
    GEL_COVERAGE_TEMPLATES,
    GEL_COVERAGE_DEFAULT_IDX,
    GEL_COVERAGE_MANUAL_IDX,
    GEL_PALM_AREA_CM2,
    resolveGelCoverageArea,
    GEL_COAPPLICATION_ORDER,
    GEL_COAPPLICATION_FACTORS,
    gelCoApplicationFactor,
    type GelCoverageTemplate,
    type GelCoApplication,
    type GelProductSpec,
    type GelKinetics,
} from './pk';
export {
    convertToPgMl,
    createCalibrationInterpolator,
    OU_DEFAULT_PARAMS,
    buildOUKalmanCalibration,
    type CalibrationModel,
    type CalibrationMode,
    type OUKalmanMode,
    type OUCalibParams,
} from './calibration';
export {
    initPersonalModel,
    computeCPAAtTimeWithTheta,
    computeE2AtTimeWithTheta,
    ekfUpdatePersonalModel,
    replayPersonalModel,
    replayPersonalModelTimeline,
    computeCPAE2InhibitionFactor,
    computeSimulationWithCI,
    PERSONAL_E2_CEILING_PGML,
    type ResidualAnchor,
    type PersonalModelState,
    type PersonalSnapshot,
    type EKFDiagnostics,
} from './personalModel';
export {
    fitMipd,
    fitMipdTimeline,
    makeMipdResolver,
    mipdPredict,
    mipdDrugE2AtTimeSorted,
    gpResidualPredict,
    MIPD_DEFAULT_PRIOR,
    MIPD_PARAM_COUNT,
    GP_RESIDUAL_DEFAULT,
    MIPD_CONSTANTS,
    type MipdPrior,
    type MipdFit,
    type MipdSnapshot,
    type MipdAnchor,
    type MipdPrediction,
    type GpResidualConfig,
} from './mipd';
// [ported] Dropped: `export { encryptData, decryptData } from './src/utils/dataEncryption'`.
// That barrel line re-exports the upstream app's own encryption helper, which is not
// part of the pharmacokinetic engine — and this app already has its own in `logic.ts`.
// Nothing in the six engine modules imports it; only this barrel listed it.

