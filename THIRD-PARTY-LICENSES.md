# Third-party licences

Components in this repository that are not our own work, and the terms they are used
under. The full text of each is reproduced or linked below.

---

## HRT Pharmacokinetic Model

This project incorporates the pharmacokinetic model
developed by Mihari.

Source:
https://github.com/LaoZhong-Mihari/HRT-Recorder-PKcomponent-Test

The model is reused under a non-commercial license granted
by the copyright holder.

Copyright © Mihari.

**Granted:** 2026-09-18, by the copyright holder, in reply to a request from this
project. The upstream repository declares no licence file of its own, so the
permission rests on that exchange rather than on a file in the upstream tree. Keep
the correspondence; it is the evidence this section refers to.

**Non-commercial is a real limit, not boilerplate.** The model may not be used in a
paid product, and this project may not be sold or monetised on the strength of it. If
that ever changes, the grant must be renegotiated with the copyright holder first.

What is derived from it, concretely — the algorithms, the mathematical models and the
parameters, following the logic of that project's `PKcore.swift` and
`PKparameter.swift`: the three-compartment models, the two-part depot kinetics, and
the sublingual absorption tiers. See `README.md` and the implementation in
`src/utils/vialLevel.ts` and `logic/`.

---

## Upstream web application

The web application this project is forked from is MIT-licensed. Its notice is
preserved in full in [`LICENSE`](LICENSE):

```
Copyright (c) 2025 Joseph Smirnova Oyama
Licensed under the MIT License.
```

MIT requires the copyright notice and permission notice to be included in all copies
or substantial portions of the software — which is why `LICENSE` stays at the
repository root unchanged and is not replaced by a licence of our own.

---

## Reicon

The icon set the whole interface is drawn from. `src/icons/index.ts` is the only file
that names a glyph, and it re-exports every one of them from this package — the app
draws no icon of its own.

Package: `reicon` 1.2.4, MIT.

Source:
https://github.com/dqev/reicon

`node_modules/reicon/package.json` declares `"license": "MIT"` and names its author
(`devchauhan`, https://devchauhan.in). The published package ships **no `LICENSE`
file and no separate copyright line**, so that field is the whole of the evidence
here — recorded rather than paraphrased, because there is nothing further to
reproduce. MIT requires the copyright notice and permission notice to be included in
all copies or substantial portions of the software.

---

## PP-OCRv6 OCR models

`public/ocr/det.onnx` and `public/ocr/rec.onnx` are the PP-OCRv6 *small* text
detector and recogniser: the models the scan feature runs locally through ONNX
Runtime Web. They are **not committed** — `scripts/sync-ocr-assets.mjs` downloads
them, which is the only supported path — so this section is the licence record for
files that are generated at build time rather than tracked.

Models: `PaddlePaddle/PP-OCRv6_small_det_onnx`, revision
`37b02eded8dbca659f8ee5d51f822ea1ebd9bcba`, and
`PaddlePaddle/PP-OCRv6_small_rec_onnx`, revision
`ba215b1cc49d9ed4459d161b96778e8643fe0c1f`.

Source (no token required):
https://www.modelscope.cn/models/PaddlePaddle/PP-OCRv6_small_det_onnx
https://www.modelscope.cn/models/PaddlePaddle/PP-OCRv6_small_rec_onnx

Upstream project: PaddleOCR, https://github.com/PaddlePaddle/PaddleOCR

Licence: **Apache License 2.0**. Both model cards declare `license: apache-2.0`,
and PaddleOCR's own `LICENSE` is the standard Apache 2.0 text. The Apache 2.0
terms are reproduced at https://www.apache.org/licenses/LICENSE-2.0 and must
travel with any redistribution of these files.

The ONNX Runtime Web runtime the models run on (`onnxruntime-web`, MIT) is an npm
dependency and is covered by the generated in-app licence page, not here.

---

## Brand marks

`src/assets/brands/x.svg` and `src/assets/brands/google.svg` are the X and Google
logos, taken byte for byte from each provider's own published SVG and used to label
the matching sign-in button (`src/icons/brand.tsx`).

They are **trademarks of their owners, not icons**, and they are not covered by
reicon's MIT licence or by any licence in this repository. They identify the provider
a button signs in with, and nothing else; do not replace either with a stand-in glyph
from the icon set, and do not recolour Google's.

---

## How this file is surfaced

- [`LICENSE`](LICENSE) carries the upstream MIT notice.
- This file carries the model grant, which is **not** MIT and must not be collapsed
  into it — a reader who assumed one licence covered the repository would be wrong in
  a way that matters commercially.
- The in-app 开源许可 page (`src/pages/LicenceSettings.tsx`) lists the dependencies
  that ship in the bundle, generated by `scripts/gen-licences.mjs`. It covers npm
  packages mechanically, reicon included; this file carries the entries that need
  prose — the model grant, which is **not** MIT, reicon's grant, whose package ships
  no licence text to quote, and the two brand marks, which are trademarks rather than
  licensed work.
